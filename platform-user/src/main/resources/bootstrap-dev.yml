spring:
  cloud:
    nacos:
      discovery:
        server-addr: http://localhost:8848
        group: ASOUL-FAN-GROUP
      config:
        server-addr: http://localhost:8848
        file-extension: yaml